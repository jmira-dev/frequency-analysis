# Frequency Analysis with p5.js

## Setup
To run this project run : `npm install` followed by `npm run start` and point your browser at `http://localhost:3000/`.

To see local changes reflected, you need to **hard reload** the page.
